[tool:pytest]
markers =
    integration: marks tests as integration tests that use real APIs and send emails (deselect with '-m "not integration"')

# By default, exclude integration tests from normal test runs
addopts = -m "not integration"